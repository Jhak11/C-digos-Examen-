#include <iostream>
#include <vector>
using namespace std;

int main() {
    
    vector<int> lista;

    // Inserción al final -> O(1) amortizado (en promedio es O(1))
    lista.push_back(5);

    // Inserción al inicio -> O(n)
    lista.insert(lista.begin(), 3);

    // Mostrar contenido
    for (int valor : lista) {
        cout << valor << " ";
    }

    return 0;
}
